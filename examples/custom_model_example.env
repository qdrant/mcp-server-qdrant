# Example configuration for using custom FastEmbed models
# Copy this file to .env or export these variables in your shell

# Basic Qdrant settings
QDRANT_URL=http://localhost:6333
COLLECTION_NAME=my_collection

# Standard model (default)
EMBEDDING_PROVIDER=fastembed
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Custom model configuration
# Set this to true to use a custom model
EMBEDDING_USE_CUSTOM_MODEL=false

# Custom model settings (only used if EMBEDDING_USE_CUSTOM_MODEL=true)
EMBEDDING_CUSTOM_MODEL_NAME=my-custom-e5-model
EMBEDDING_CUSTOM_HF_MODEL_ID=intfloat/multilingual-e5-small
EMBEDDING_CUSTOM_POOLING_TYPE=MEAN
EMBEDDING_CUSTOM_NORMALIZATION=true
EMBEDDING_CUSTOM_VECTOR_DIMENSION=384
EMBEDDING_CUSTOM_MODEL_FILE=onnx/model.onnx

# Alternative: Use a direct URL instead of HuggingFace
# EMBEDDING_CUSTOM_MODEL_URL=https://myserver.com/models/my-model.tar.gz
